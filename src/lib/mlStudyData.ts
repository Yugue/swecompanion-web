// AUTO-GENERATED by scripts/port-ml-study-data.mjs from lib/ml_study_data.dart.
// Quiz ANSWERS are intentionally not included here (see scripts/quizAnswers.json) -
// they're premium content served only from Firestore. See the plan's "Premium quiz architecture".

export interface MlTopic {
  id: string;
  title: string;
  summary: string;
  keyPoints: string[];
  interviewPrompt: string;
  code?: string;
}

export interface MlQuizQuestion {
  question: string;
}

export interface MlPart {
  id: string;
  number: string;
  title: string;
  description: string;
  color: string;
  icon: string;
  topics: MlTopic[];
  quiz: MlQuizQuestion[];
}

export const mlParts: MlPart[] = [
  {
    id: "foundations",
    number: "1",
    title: "Foundations",
    description: "The mathematical and modeling vocabulary needed to reason from first principles.",
    color: "#4285F4",
    icon: "Blocks",
    topics: [
      {
        id: "ml-fundamentals",
        title: "ML fundamentals",
        summary: "Machine learning learns a mapping from data; the real goal is performance on unseen examples, not memorizing the training set.",
        keyPoints: ["Supervised learning uses labels; unsupervised learning finds structure; self-supervised learning derives targets from the data itself.", "Parameters are learned. Hyperparameters—learning rate, batch size, depth—are chosen.", "Use train data to fit, validation data to choose, and the test set once for final evaluation.", "Underfitting is high bias; overfitting is high variance. Leakage gives the model information unavailable at prediction time."],
        interviewPrompt: "Given a widening train–validation gap, identify whether capacity, data, leakage, or distribution shift is the likely cause.",
        code: "model.fit(x_train, y_train)  # tune on validation, never test",
      },
      {
        id: "linear-algebra",
        title: "Linear algebra essentials",
        summary: "Neural networks are compositions of linear transformations and nonlinearities; shapes explain what every operation can do.",
        keyPoints: ["A tensor generalizes scalars, vectors, and matrices; always state its shape.", "A dot product measures aligned magnitude. Matrix multiplication applies many dot products at once.", "Norms measure size; cosine similarity measures direction rather than magnitude.", "Eigenvectors keep their direction under a linear transformation; useful intuition, rarely a long interview derivation."],
        interviewPrompt: "Reason through the shape of XW when X is [batch, features] and W is [features, hidden].",
        code: "hidden = x @ weight  # [B, D] @ [D, H] -> [B, H]",
      },
      {
        id: "calculus",
        title: "Calculus essentials",
        summary: "A gradient tells how a small change in every parameter changes a scalar loss.",
        keyPoints: ["A partial derivative changes one input while holding the others fixed.", "The gradient collects all partial derivatives and points toward steepest increase.", "The chain rule multiplies local sensitivities through a computation graph.", "A Jacobian generalizes derivatives for vector-valued functions; backprop efficiently computes vector–Jacobian products."],
        interviewPrompt: "Explain what ∂L/∂W means and why its shape must match W.",
        code: "loss.backward()  # autograd applies the chain rule",
      },
      {
        id: "probability-statistics",
        title: "Probability and statistics essentials",
        summary: "Probability models uncertainty; statistics helps estimate and evaluate behavior from finite samples.",
        keyPoints: ["Expectation is a probability-weighted average; variance measures spread around it.", "Bernoulli models a binary outcome, categorical models one of many classes, and Gaussian models continuous variation.", "Bayes’ rule reverses a conditional using a prior and likelihood.", "Maximum likelihood chooses parameters that make observed data probable; log-likelihood turns products into sums."],
        interviewPrompt: "Connect maximum likelihood to cross-entropy for classification.",
        code: "log_likelihood = distribution.log_prob(target).sum()",
      },
      {
        id: "neuron-mlp",
        title: "Neuron → Multi-Layer Perceptron",
        summary: "A neuron computes a weighted sum plus bias; stacking layers with nonlinearities learns hierarchical representations.",
        keyPoints: ["A linear layer is z = Wx + b; the bias shifts the decision boundary.", "Width controls units per layer; depth controls the number of successive transformations.", "Without nonlinear activations, many linear layers collapse into one linear map.", "The output head must match the task: one logit for binary, C logits for C classes, continuous values for regression."],
        interviewPrompt: "Compute parameter count and output shape for a two-layer network.",
        code: "net = nn.Sequential(nn.Linear(d, h), nn.ReLU(), nn.Linear(h, c))",
      },
      {
        id: "activations",
        title: "Activation functions",
        summary: "Activations introduce nonlinearity and shape the gradient flow through a network.",
        keyPoints: ["ReLU is simple and usually effective, but negative units can die; Leaky ReLU preserves a small negative slope.", "Sigmoid maps to (0, 1) and suits binary probabilities, but saturates in deep hidden layers.", "Tanh is zero-centered but also saturates. Gaussian Error Linear Unit (GELU) is a smooth gate common in Transformers.", "Rule of thumb: ReLU-family for standard hidden layers, GELU for Transformers, sigmoid only where bounded output is required."],
        interviewPrompt: "Why can a network with only linear layers not represent XOR?",
        code: "x = torch.nn.functional.gelu(x)",
      },
      {
        id: "loss-functions",
        title: "Loss functions",
        summary: "A loss is the differentiable training objective; a metric is the human-facing measure used to judge success.",
        keyPoints: ["Mean Squared Error (MSE) is common for regression and strongly penalizes large errors.", "Binary cross-entropy with logits combines sigmoid and a stable log-loss.", "Multiclass cross-entropy consumes raw logits and a class index; do not apply softmax first.", "Mean Absolute Error is robust to outliers; Huber is quadratic near zero and linear for large errors."],
        interviewPrompt: "Choose a loss and evaluation metric for a highly imbalanced fraud classifier.",
        code: "loss = F.cross_entropy(logits, class_ids)",
      },
      {
        id: "forward-propagation",
        title: "Forward propagation",
        summary: "The forward pass applies the model’s current parameters to inputs and records operations needed for learning.",
        keyPoints: ["Track batch and feature dimensions at every layer.", "Forward propagation computes predictions but does not update weights.", "Training mode enables behavior such as dropout and Batch Normalization updates; evaluation mode disables or freezes them.", "Inference usually adds no-grad mode to avoid storing a backward graph."],
        interviewPrompt: "Explain the difference among forward pass, inference, train mode, and eval mode.",
        code: "with torch.no_grad(): prediction = model(x)",
      },
      {
        id: "backpropagation",
        title: "Backpropagation + chain rule",
        summary: "Backpropagation reuses intermediate derivatives to propagate loss sensitivity from outputs to every parameter.",
        keyPoints: ["Forward: compute activations and loss. Backward: apply the chain rule in reverse topological order.", "At a branch, gradient contributions add because one value influenced the loss through multiple paths.", "Gradients accumulate by default in PyTorch, so clear them before the next update.", "The optimizer uses gradients; backprop itself does not change parameters."],
        interviewPrompt: "Walk through ∂L/∂W₁ in a two-layer network without hiding behind “autograd does it.”",
        code: "optimizer.zero_grad(); loss.backward(); optimizer.step()",
      },
    ],
    quiz: [{ question: "Why are nonlinear activations necessary between linear layers?" }, { question: "X has shape [32, 128] and W has shape [128, 64]. What is XW?" }, { question: "What should Binary Cross-Entropy with Logits receive?" }, { question: "What is the difference between a logit and a probability?" }, { question: "What does backpropagation compute?" }, { question: "What does a positive ∂L/∂w tell you?" }, { question: "Why does cross-entropy heavily punish a confident wrong prediction?" }, { question: "Training loss falls while validation loss rises. What is happening?" }, { question: "How do high bias and high variance differ?" }, { question: "Training loss does not decrease. What do you check first?" }],
  },
  {
    id: "learning",
    number: "2",
    title: "How neural networks learn",
    description: "Optimization, stability, regularization, and the evidence used to debug training.",
    color: "#EA4335",
    icon: "TrendingDown",
    topics: [
      {
        id: "gradient-descent",
        title: "Gradient descent",
        summary: "Gradient descent iteratively moves parameters opposite the loss gradient.",
        keyPoints: ["Update rule: θ ← θ − η∇L; η is the learning rate.", "Too large can oscillate or diverge; too small makes little progress.", "The gradient is local, so deep-learning objectives do not guarantee the global optimum."],
        interviewPrompt: "Sketch what the loss curve looks like when the learning rate is too high.",
        code: "for p in model.parameters(): p -= lr * p.grad",
      },
      {
        id: "mini-batches",
        title: "Stochastic and mini-batch training",
        summary: "Mini-batches trade exact gradients for efficient, noisy estimates.",
        keyPoints: ["Full-batch uses all examples per update; stochastic gradient descent uses one; mini-batch uses a practical middle ground.", "Smaller batches use less memory and add noise; larger batches improve accelerator utilization and gradient stability.", "Changing batch size often requires retuning the learning rate."],
        interviewPrompt: "Compare batch size 16 with 4096 for memory, noise, throughput, and generalization.",
        code: "for x, y in loader: loss_fn(model(x), y).backward()",
      },
      {
        id: "optimizers",
        title: "Momentum, RMSProp, Adam, and AdamW",
        summary: "Modern optimizers reshape raw gradients to move faster and more stably.",
        keyPoints: ["Momentum keeps an exponential moving average of gradients to smooth oscillation.", "RMSProp scales each coordinate using recent squared gradients.", "Adam combines first- and second-moment estimates with bias correction.", "AdamW decouples weight decay from the adaptive gradient update and is the common default for Transformers."],
        interviewPrompt: "Explain what Adam adds beyond momentum and why AdamW differs from Adam plus L2.",
        code: "optimizer = torch.optim.AdamW(model.parameters(), lr=3e-4)",
      },
      {
        id: "learning-rates",
        title: "Learning rates and schedules",
        summary: "The learning rate controls step size; schedules change it as training moves from exploration to refinement.",
        keyPoints: ["Warmup starts small and increases, protecting unstable early optimization.", "Decay lowers the rate later to settle into a good solution; cosine decay is a smooth common choice.", "Fine-tuning usually needs a much smaller rate than training from scratch."],
        interviewPrompt: "Distinguish warmup from decay and say when each helps.",
        code: "scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(opt, T_max=steps)",
      },
      {
        id: "initialization",
        title: "Weight initialization",
        summary: "Initialization must break symmetry while keeping activation and gradient scales healthy across layers.",
        keyPoints: ["All-zero weights make units learn identically.", "Xavier/Glorot initialization suits tanh-like activations.", "He/Kaiming initialization accounts for ReLU dropping roughly half of activations.", "Initialization matters less after training succeeds, but can decide whether training starts at all."],
        interviewPrompt: "Why is zero initialization acceptable for some biases but not for all weights?",
        code: "torch.nn.init.kaiming_normal_(layer.weight, nonlinearity=\"relu\")",
      },
      {
        id: "gradient-pathologies",
        title: "Vanishing and exploding gradients",
        summary: "Repeated Jacobian multiplication can shrink or grow gradients exponentially through depth or time.",
        keyPoints: ["Vanishing gradients prevent early layers from learning long-range dependencies.", "Exploding gradients cause unstable updates, very large loss, or NaNs.", "Residual paths, normalization, gating, and good initialization improve flow; clipping specifically limits explosions."],
        interviewPrompt: "A recurrent model produces NaNs after long sequences—diagnose before changing architecture.",
        code: "total_norm = torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)",
      },
      {
        id: "gradient-clipping",
        title: "Gradient clipping",
        summary: "Clipping caps an update when the gradient norm becomes dangerously large.",
        keyPoints: ["Norm clipping rescales the entire gradient vector only when its norm exceeds a threshold.", "It stabilizes training but does not fix the source of chronic explosions.", "Clipping is an optimization safeguard, not a regularizer whose purpose is better validation performance."],
        interviewPrompt: "Why can clipping stop NaNs but fail to improve a poorly specified model?",
        code: "torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)",
      },
      {
        id: "regularization",
        title: "Regularization",
        summary: "Regularization biases learning toward solutions likely to generalize beyond the training sample.",
        keyPoints: ["L2/weight decay discourages large weights; L1 encourages sparsity.", "Early stopping limits memorization; data augmentation injects valid invariances.", "Label smoothing prevents overconfident targets.", "Optimization fixes poor training fit; regularization primarily targets the train–validation gap."],
        interviewPrompt: "Pick interventions for low train accuracy versus excellent train but poor validation accuracy.",
        code: "loss = data_loss + weight_decay * weight.square().sum()",
      },
      {
        id: "dropout",
        title: "Dropout",
        summary: "Dropout randomly removes activations during training so the network cannot rely on one fragile path.",
        keyPoints: ["During training, units are dropped and survivors are scaled; at inference, dropout is disabled.", "It can reduce overfitting but may slow optimization.", "It is less central in some modern normalized architectures, but remains a useful regularizer."],
        interviewPrompt: "Why must model.train() and model.eval() produce different dropout behavior?",
        code: "x = F.dropout(x, p=0.1, training=self.training)",
      },
      {
        id: "normalization",
        title: "BatchNorm, LayerNorm, and RMSNorm",
        summary: "Normalization stabilizes activation scale, but the normalized axes and inference behavior differ.",
        keyPoints: ["Batch Normalization uses batch statistics per feature and running statistics at inference; small batches can be noisy.", "Layer Normalization normalizes features within each example and works naturally for variable-length sequences.", "Root Mean Square Normalization (RMSNorm) scales by root-mean-square magnitude without subtracting the mean.", "All learn a scale; BatchNorm and LayerNorm also commonly learn a shift."],
        interviewPrompt: "For a Transformer token tensor [B, N, D], explain why LayerNorm over D is preferred.",
        code: "x = torch.nn.LayerNorm(hidden_size)(x)",
      },
      {
        id: "residuals",
        title: "Residual connections",
        summary: "A residual block learns a change F(x) around an identity route: y = x + F(x).",
        keyPoints: ["The identity path lets information and gradients bypass difficult transformations.", "Residuals make very deep networks easier to optimize; they do not merely add representational capacity.", "Shapes must match, or a projection is needed."],
        interviewPrompt: "Explain why an identity path helps even if F initially learns almost zero.",
        code: "x = x + block(norm(x))",
      },
      {
        id: "bias-variance",
        title: "Overfitting, underfitting, and bias–variance",
        summary: "Training and validation curves reveal whether the bottleneck is fitting the data or generalizing.",
        keyPoints: ["High training and validation error suggests underfitting/high bias.", "Low training error with worse validation error suggests overfitting/high variance.", "More capacity can reduce bias; more representative data and regularization can reduce variance."],
        interviewPrompt: "Read a pair of learning curves and choose the smallest evidence-based intervention.",
        code: "gap = validation_loss - training_loss",
      },
      {
        id: "training-diagnostics",
        title: "Training diagnostics and debugging",
        summary: "Debug the pipeline before tuning the model: prove each component works in a controlled setting.",
        keyPoints: ["Overfit a tiny batch to test model, loss, labels, and optimizer wiring.", "Inspect gradient norms, activation distributions, learning rate, and examples—not only aggregate loss.", "Compare against a simple baseline and change one variable at a time.", "NaNs suggest numerical instability; a flat loss suggests broken gradients, tiny steps, or mismatched targets."],
        interviewPrompt: "Give a prioritized checklist for a model stuck at random accuracy.",
        code: "assert model(tiny_x).shape == tiny_y_expected_shape",
      },
      {
        id: "generalization-validation",
        title: "Generalization and validation",
        summary: "Validation estimates performance on the deployment distribution and guides decisions without contaminating the final test.",
        keyPoints: ["Split by the real generalization boundary—often time, user, or entity—not blindly at random.", "Repeated test-set decisions overfit the test set.", "Cross-validation helps when data is limited; distribution shift can invalidate a clean random split."],
        interviewPrompt: "Design a leakage-safe split for recommendations or fraud with repeated users over time.",
        code: "train = data[data.time < cutoff]; valid = data[data.time >= cutoff]",
      },
    ],
    quiz: [{ question: "What does an extremely small learning rate look like?" }, { question: "Loss becomes NaN and gradient norms spike. What immediate safeguard helps?" }, { question: "What does momentum remember?" }, { question: "What does Adam add to momentum?" }, { question: "Warmup and decay solve what different problems?" }, { question: "Why does He initialization suit ReLU?" }, { question: "BatchNorm or LayerNorm for a Transformer—and why?" }, { question: "What changes for dropout at inference?" }, { question: "Why do residual connections improve optimization?" }, { question: "Train accuracy is poor and validation accuracy is similarly poor. Add regularization?" }],
  },
  {
    id: "architectures",
    number: "3",
    title: "Core architectures",
    description: "The progression from spatial inductive bias to recurrent memory and attention.",
    color: "#FBBC04",
    icon: "GitBranch",
    topics: [
      {
        id: "cnn",
        title: "Convolutional Neural Network fundamentals",
        summary: "A Convolutional Neural Network (CNN) applies learned local filters across space, reusing the same parameters at every location.",
        keyPoints: ["Local connectivity matches spatial structure.", "Weight sharing makes CNNs parameter-efficient and translation equivariant.", "Early filters learn edges; deeper layers combine them into larger patterns.", "Input and output channels determine how filters mix feature maps."],
        interviewPrompt: "Why is a CNN usually a stronger image baseline than a similarly sized fully connected network?",
        code: "conv = nn.Conv2d(in_channels=3, out_channels=64, kernel_size=3)",
      },
      {
        id: "cnn-geometry",
        title: "Receptive fields, stride, padding, and pooling",
        summary: "CNN geometry controls output resolution and how much of the original input each feature can see.",
        keyPoints: ["Output size per axis is floor((N + 2P − K)/S) + 1.", "Stride downsamples; padding preserves border information or size; dilation expands coverage without more kernel weights.", "The receptive field grows across stacked layers.", "Pooling aggregates neighborhoods and adds some invariance, but discards precise location."],
        interviewPrompt: "Calculate output shape and effective receptive field for two 3×3 convolutions.",
        code: "out = (size + 2 * padding - kernel) // stride + 1",
      },
      {
        id: "resnet",
        title: "ResNet as a CNN architecture",
        summary: "ResNet applies residual blocks to make deep convolutional feature extractors trainable.",
        keyPoints: ["A block learns F(x) and returns x + F(x).", "Projection shortcuts reconcile changed channel counts or spatial sizes.", "The main point is optimization and gradient flow, not memorizing named ResNet depths."],
        interviewPrompt: "When does a ResNet shortcut need a learned projection?",
        code: "return projection(x) + conv_block(x)",
      },
      {
        id: "rnn",
        title: "Recurrent Neural Network fundamentals",
        summary: "A Recurrent Neural Network (RNN) processes a sequence one step at a time, carrying a hidden state.",
        keyPoints: ["Typical recurrence: hₜ = f(Wₓxₜ + Wₕhₜ₋₁ + b).", "Weights are shared across time.", "Sequential dependence limits parallelism and makes long-range information hard to preserve."],
        interviewPrompt: "State the hidden-state equation and explain what is shared over time.",
        code: "h = torch.tanh(x_t @ W_x + h @ W_h + b)",
      },
      {
        id: "bptt",
        title: "Backpropagation Through Time",
        summary: "Backpropagation Through Time (BPTT) unfolds recurrence into a deep graph and applies ordinary backprop across timesteps.",
        keyPoints: ["The same recurrent weights receive gradient contributions from every use.", "Many Jacobian products cause vanishing or exploding gradients.", "Truncated BPTT limits how far gradients travel to control compute and instability."],
        interviewPrompt: "Why can an RNN remember a signal forward while still failing to learn a long-range dependency?",
        code: "loss.backward()  # graph includes the unrolled recurrent steps",
      },
      {
        id: "lstm-gru",
        title: "Long Short-Term Memory and Gated Recurrent Unit",
        summary: "Gated recurrent cells create controlled memory paths that preserve useful information over longer spans.",
        keyPoints: ["Long Short-Term Memory (LSTM) separates cell state cₜ from hidden state hₜ and uses forget, input, and output gates.", "The cell’s additive update improves gradient flow.", "A Gated Recurrent Unit (GRU) is simpler and has no separate cell state; it uses update and reset gates.", "Gating helps but recurrent computation remains sequential."],
        interviewPrompt: "Contrast the roles of cₜ and hₜ, then explain the structural difference in a GRU.",
        code: "output, (h_n, c_n) = nn.LSTM(d, h)(sequence)",
      },
      {
        id: "seq2seq",
        title: "Sequence-to-sequence models",
        summary: "A sequence-to-sequence model maps an input sequence to an output sequence, often through encoder and decoder components.",
        keyPoints: ["The encoder builds a source representation; the decoder predicts tokens autoregressively.", "A single fixed vector creates an information bottleneck for long inputs.", "Teacher forcing feeds the true previous target during training; inference must feed the model’s own previous output.", "Exposure bias comes from this train–inference mismatch."],
        interviewPrompt: "Explain teacher forcing and why it can make training easier but inference brittle.",
        code: "decoder_input = target[:, :-1]  # shifted targets during training",
      },
    ],
    quiz: [{ question: "Why are CNNs parameter-efficient?" }, { question: "What is the convolution output-size formula?" }, { question: "What is a receptive field?" }, { question: "What does pooling trade away?" }, { question: "State a basic RNN hidden-state recurrence." }, { question: "Why does BPTT suffer vanishing or exploding gradients?" }, { question: "What are the two LSTM states?" }, { question: "What is the key structural difference between GRU and LSTM?" }, { question: "What is teacher forcing?" }, { question: "What problem did encoder–decoder attention solve?" }],
  },
  {
    id: "transformers",
    number: "4",
    title: "Transformers in depth",
    description: "Attention mechanics, block structure, model families, and the cost of long context.",
    color: "#34A853",
    icon: "Share2",
    topics: [
      {
        id: "qkv",
        title: "Self-attention + Query / Key / Value",
        summary: "Self-attention lets every token retrieve information from other tokens in the same sequence.",
        keyPoints: ["Query is what this token is looking for.", "Key is what each token can be matched against.", "Value is the information retrieved when a match is strong.", "Scores come from QKᵀ; softmax weights are applied to V."],
        interviewPrompt: "Explain Q, K, and V without saying only that they are learned projections.",
        code: "attention = softmax(Q @ K.transpose(-2, -1) / sqrt(d_k)) @ V",
      },
      {
        id: "scaled-multihead",
        title: "Scaled dot-product + multi-head attention",
        summary: "Dot products measure query–key compatibility; scaling stabilizes softmax, and multiple heads learn different relations.",
        keyPoints: ["Dot-product variance grows with key dimension dₖ.", "Dividing by √dₖ keeps logits in a range where softmax has useful gradients.", "Each head has its own projections and subspace; outputs are concatenated and projected.", "Heads can specialize, but their roles are learned rather than assigned."],
        interviewPrompt: "Why does omitting √dₖ make training harder as head dimension grows?",
        code: "scores = (q @ k.transpose(-2, -1)) * (q.size(-1) ** -0.5)",
      },
      {
        id: "masking",
        title: "Attention masking and causal masking",
        summary: "Masks remove invalid attention edges before softmax.",
        keyPoints: ["Padding masks stop real tokens from attending to padding.", "A causal mask blocks future positions so autoregressive training cannot leak the answer.", "Use a very negative score before softmax, not a zero probability afterward.", "Encoder self-attention is usually bidirectional; decoder self-attention is causal."],
        interviewPrompt: "What information leakage occurs if a next-token model omits its causal mask?",
        code: "scores = scores.masked_fill(mask == 0, float(\"-inf\"))",
      },
      {
        id: "positional",
        title: "Positional encoding and embeddings",
        summary: "Because attention alone is permutation-equivariant, models must inject token order.",
        keyPoints: ["Sinusoidal encodings are deterministic; learned position embeddings are trainable table entries.", "Position signals are combined with token representations before or within attention.", "Absolute schemes encode where a token is; relative schemes emphasize distances between tokens."],
        interviewPrompt: "Why can self-attention not distinguish “dog bites man” from a permutation without position information?",
        code: "x = token_embedding(ids) + position_embedding(positions)",
      },
      {
        id: "rope",
        title: "Rotary Position Embedding",
        summary: "Rotary Position Embedding (RoPE) rotates query and key feature pairs so their dot product carries relative-position information.",
        keyPoints: ["The rotation angle depends on token position and frequency.", "Applying it to Q and K makes attention depend naturally on relative offsets.", "It does not add a position vector directly to values.", "Extrapolation still depends on training range and scaling choices."],
        interviewPrompt: "What is rotated in RoPE, and how does that affect QKᵀ?",
        code: "q, k = apply_rotary_pos_emb(q, k, cos, sin)",
      },
      {
        id: "transformer-block",
        title: "The Transformer block",
        summary: "A block alternates token mixing through attention and per-token feature transformation through a feed-forward network.",
        keyPoints: ["Attention mixes information across sequence positions.", "The feed-forward network applies the same Multi-Layer Perceptron independently to each token.", "Residual connections preserve an identity route; normalization stabilizes scale.", "In Pre-Layer Normalization: x ← x + Attention(LN(x)), then x ← x + FFN(LN(x))."],
        interviewPrompt: "Identify which component mixes tokens and which mixes features.",
        code: "x = x + attn(norm1(x)); x = x + ffn(norm2(x))",
      },
      {
        id: "pre-post-ln",
        title: "Pre-LayerNorm vs Post-LayerNorm",
        summary: "Normalization placement changes the residual path and optimization behavior.",
        keyPoints: ["Pre-LN uses x + F(LN(x)), leaving a clean identity route.", "Post-LN uses LN(x + F(x)).", "Pre-LN generally trains deep Transformers more reliably; Post-LN can require careful warmup.", "The distinction is placement, not a different normalization formula."],
        interviewPrompt: "Write both block equations and point to the clean identity path.",
        code: "pre_ln = x + sublayer(layer_norm(x))",
      },
      {
        id: "encoder-only",
        title: "Encoder-only Transformers",
        summary: "Encoder-only models build bidirectional contextual representations of an observed sequence.",
        keyPoints: ["Every non-padding token can attend to tokens on both sides.", "They suit classification, tagging, retrieval, and masked-token pretraining.", "A task head reads a pooled token or token-level outputs.", "BERT is the canonical example."],
        interviewPrompt: "Why is an encoder-only model unsuitable for unrestricted next-token generation without changing its mask/objective?",
        code: "features = encoder(input_ids, attention_mask=mask)",
      },
      {
        id: "decoder-only",
        title: "Decoder-only Transformers",
        summary: "Decoder-only models predict the next token using causal self-attention.",
        keyPoints: ["Each position can see only itself and earlier tokens.", "Training predicts many next-token targets in parallel despite the causal dependency.", "Generation is sequential and benefits from a Key-Value cache.", "GPT is the canonical family."],
        interviewPrompt: "How can decoder training be parallel while autoregressive inference is sequential?",
        code: "loss = F.cross_entropy(logits[:, :-1].flatten(0, 1), ids[:, 1:].flatten())",
      },
      {
        id: "encoder-decoder",
        title: "Encoder–decoder Transformers",
        summary: "An encoder represents the source; a causal decoder generates the target and cross-attends to encoder outputs.",
        keyPoints: ["Encoder self-attention is bidirectional.", "Decoder self-attention is causal.", "Cross-attention uses decoder queries with encoder keys and values.", "This structure is natural for translation and input-conditioned generation."],
        interviewPrompt: "For cross-attention, which component supplies Q and which supplies K/V?",
        code: "decoded = decoder(target, memory=encoder(source))",
      },
      {
        id: "bert-gpt-t5",
        title: "BERT vs GPT vs T5",
        summary: "These families mainly differ in block structure, pretraining objective, and the tasks they naturally support.",
        keyPoints: ["BERT: encoder-only, bidirectional, originally masked-language modeling.", "GPT: decoder-only, causal next-token prediction.", "T5: encoder–decoder, text-to-text span-corruption pretraining.", "Choose based on input/output structure and constraints, not brand recency."],
        interviewPrompt: "Choose among BERT, GPT, and T5 for tagging, free generation, and translation.",
        code: "architecture = {\"tagging\": \"encoder\", \"generation\": \"decoder\", \"translation\": \"encoder-decoder\"}",
      },
      {
        id: "transformer-complexity",
        title: "Transformer compute and memory complexity",
        summary: "Dense attention creates an N×N score matrix, so sequence length is the dominant long-context cost.",
        keyPoints: ["Q is [N, d], Kᵀ is [d, N], and QKᵀ is [N, N].", "Attention compute is O(N²d); score memory is O(N²) per layer/head grouping.", "Doubling sequence length roughly quadruples the attention portion.", "The feed-forward network often dominates FLOPs at shorter contexts; both matter."],
        interviewPrompt: "What changes when context length doubles, and which tensors cause it?",
        code: "scores = q @ k.transpose(-2, -1)  # [..., N, N]",
      },
    ],
    quiz: [{ question: "Define Query, Key, and Value." }, { question: "What operation produces attention weights?" }, { question: "Why divide by √dₖ?" }, { question: "Why use multiple attention heads?" }, { question: "What does a causal mask guarantee?" }, { question: "What problem does positional information solve?" }, { question: "Which Transformer component mixes positions, and which mixes features?" }, { question: "State the Pre-LN residual form." }, { question: "Where do Q, K, and V come from in cross-attention?" }, { question: "Why is dense attention quadratic in sequence length?" }],
  },
  {
    id: "modern-deep-learning",
    number: "5",
    title: "Modern deep-learning fundamentals",
    description: "Representation learning, adaptation, decoding, and efficient autoregressive inference.",
    color: "#A78BFA",
    icon: "Sparkles",
    topics: [
      {
        id: "embeddings",
        title: "Embeddings and representation learning",
        summary: "An embedding is a learned dense vector, not the integer token identifier used to look it up.",
        keyPoints: ["Token ID → embedding table → initial vector.", "After Transformer layers, the vector becomes contextual: the same token can represent different meanings in different sentences.", "Useful representations place semantically related inputs near one another under a suitable similarity measure."],
        interviewPrompt: "Distinguish a token ID, an input embedding, and a contextual hidden state.",
        code: "x = embedding_table[token_ids]",
      },
      {
        id: "self-supervised-pretraining",
        title: "Self-supervised learning + pretraining",
        summary: "Self-supervision creates prediction targets from raw data, enabling learning from data without manual labels.",
        keyPoints: ["Next-token prediction uses earlier tokens to predict the next token.", "Masked prediction reconstructs hidden pieces from context.", "Pretraining learns broadly reusable patterns at scale; its objective is a proxy for downstream usefulness.", "Autoregressive likelihood factorizes P(x₁…xₙ) into conditional next-token probabilities."],
        interviewPrompt: "Why is next-token prediction self-supervised rather than unsupervised in the vague sense?",
        code: "inputs, targets = tokens[:, :-1], tokens[:, 1:]",
      },
      {
        id: "transfer-finetuning",
        title: "Transfer learning + fine-tuning",
        summary: "Transfer learning reuses pretrained representations; fine-tuning adapts them to a new distribution or objective.",
        keyPoints: ["Full fine-tuning can update every parameter.", "Feature extraction freezes the backbone and trains a new head.", "Use smaller learning rates to avoid destroying useful pretrained structure.", "Domain mismatch, limited labels, and catastrophic forgetting shape the strategy."],
        interviewPrompt: "When would you freeze layers, fully fine-tune, or choose a parameter-efficient method?",
        code: "for p in backbone.parameters(): p.requires_grad = False",
      },
      {
        id: "instruction-tuning",
        title: "Instruction tuning",
        summary: "Instruction tuning is supervised fine-tuning on diverse instruction–response pairs so a model learns to follow requested behavior.",
        keyPoints: ["The loss can still be ordinary next-token cross-entropy.", "Diversity teaches the format and intent of instructions, not only one task.", "It improves controllability but does not guarantee factuality or alignment to every preference."],
        interviewPrompt: "How does instruction tuning differ from base-model pretraining?",
        code: "loss = causal_lm(instruction_plus_response, labels=response_masked_labels).loss",
      },
      {
        id: "lora",
        title: "Low-Rank Adaptation + parameter-efficient fine-tuning",
        summary: "Low-Rank Adaptation (LoRA) freezes a pretrained weight W and learns a compact update ΔW = BA.",
        keyPoints: ["A and B are trainable low-rank matrices with rank r much smaller than model dimension d.", "The effective weight is W′ = W + BA.", "It reduces trainable parameters and optimizer memory; the base weights remain reusable.", "Parameter-Efficient Fine-Tuning (PEFT) describes the broader family; it is not the same as full fine-tuning."],
        interviewPrompt: "Where does LoRA save memory, and what remains expensive during the forward pass?",
        code: "output = x @ W.T + (x @ A.T) @ B.T",
      },
      {
        id: "decoding",
        title: "Decoding strategies",
        summary: "Decoding converts next-token probabilities into a sequence, trading likelihood, diversity, latency, and repetition.",
        keyPoints: ["Greedy decoding selects the highest-probability token each step.", "Beam search keeps the best k partial sequences, expands them, and retains the best k by cumulative score.", "Sampling draws according to probabilities; high-probability tokens remain more likely.", "Temperature uses softmax(z/T): T<1 sharpens, T>1 flattens.", "Top-k keeps a fixed candidate count; top-p keeps the smallest variable set whose cumulative probability reaches p."],
        interviewPrompt: "Compare greedy, beam, temperature, top-k, and top-p for a deterministic task versus creative generation.",
        code: "probs = torch.softmax(logits / temperature, dim=-1)",
      },
      {
        id: "kv-cache",
        title: "Key-Value cache basics",
        summary: "Autoregressive inference caches past attention Keys and Values so old tokens are not projected again at every step.",
        keyPoints: ["The new token computes Qnew, Knew, and Vnew.", "Qnew attends to all cached K/V plus the new K/V.", "Old Queries are not cached because only the new position needs an output.", "Cache memory grows O(N) with context length per layer, batch, and KV head."],
        interviewPrompt: "Why cache K/V but not Q, and what latency–memory tradeoff does this create?",
        code: "keys = torch.cat([cached_keys, key_new], dim=-2)",
      },
    ],
    quiz: [{ question: "Is token ID 1532 an embedding?" }, { question: "What makes next-token prediction self-supervised?" }, { question: "Does fine-tuning always update fewer parameters?" }, { question: "What is instruction tuning?" }, { question: "Write the LoRA update." }, { question: "What exactly does beam search retain?" }, { question: "What does temperature do mathematically?" }, { question: "How do top-k and top-p differ?" }, { question: "What does the KV cache store?" }, { question: "What is catastrophic forgetting?" }],
  },
  {
    id: "practical-reasoning",
    number: "6",
    title: "Practical ML reasoning",
    description: "How to choose, diagnose, deploy, and defend an ML system under real constraints.",
    color: "#22D3EE",
    icon: "Wrench",
    topics: [
      {
        id: "hyperparameter-tuning",
        title: "Hyperparameter tuning",
        summary: "Tune the few choices with the largest effect using validation evidence and a controlled search budget.",
        keyPoints: ["Start with learning rate, then batch size, optimizer/schedule, regularization, and capacity.", "Random search usually explores important dimensions better than a dense grid.", "Bayesian and bandit methods help when evaluations are expensive.", "Track every run and change one hypothesis at a time."],
        interviewPrompt: "Given ten runs of budget, design a search that does more than blindly grid every parameter.",
        code: "trial.suggest_float(\"lr\", 1e-5, 1e-2, log=True)",
      },
      {
        id: "model-selection",
        title: "Model selection",
        summary: "Choose the model that best satisfies the real objective on validation data—not the model with the best training score.",
        keyPoints: ["The selection metric must reflect business costs such as precision, recall, calibration, or ranking quality.", "Keep the test set outside repeated decisions.", "Prefer the simplest model that meets constraints; compare against a baseline.", "Account for latency, memory, interpretability, robustness, and maintenance—not only accuracy."],
        interviewPrompt: "Two models trade 1% recall for 10× lower latency. How do you decide?",
        code: "best = max(candidates, key=lambda m: validation_utility(m))",
      },
      {
        id: "error-analysis",
        title: "Error analysis",
        summary: "Inspect mistakes and organize them into actionable failure modes before changing architecture.",
        keyPoints: ["Manually review a representative sample of false positives and false negatives.", "Slice by class, subgroup, time, length, quality, and confidence.", "Separate label errors, missing coverage, preprocessing bugs, ambiguity, and genuine model limitations.", "Prioritize categories by frequency × fixability × product value."],
        interviewPrompt: "A classifier is 90% accurate. What evidence do you collect before making it larger?",
        code: "errors = examples[predictions != labels]",
      },
      {
        id: "architecture-choice",
        title: "Choosing an architecture for a problem",
        summary: "Match the model’s inductive bias to data modality, data volume, and deployment constraints, starting from a credible baseline.",
        keyPoints: ["Tabular: linear/logistic or boosted trees are strong baselines.", "Images: CNN or Vision Transformer; text: Transformer; graphs: graph models; simple numeric inputs: linear model or MLP.", "More modern is not automatically better.", "Data quality and pretrained availability can matter more than architecture novelty."],
        interviewPrompt: "Defend a model choice for tabular churn, image defects, and text classification.",
        code: "baseline = LogisticRegression().fit(x_train, y_train)",
      },
      {
        id: "training-inference",
        title: "Training vs inference tradeoffs",
        summary: "Training learns parameters with forward, backward, and optimizer state; inference only executes predictions.",
        keyPoints: ["Training stores activations, gradients, and optimizer moments, so it consumes much more memory.", "Gradient checkpointing saves memory by recomputing activations.", "Inference can use batching, quantization, compilation, caching, and lower precision.", "Autoregressive inference is sequential and may be dominated by memory bandwidth and KV-cache size."],
        interviewPrompt: "Why can a model fit for inference but not training on the same device?",
        code: "model.eval(); prediction = model(x)  # no optimizer state or backward graph",
      },
      {
        id: "system-tradeoffs",
        title: "Latency, memory, and accuracy tradeoffs",
        summary: "Production model choice is constrained optimization: maximize useful quality while meeting resource and reliability budgets.",
        keyPoints: ["Latency is time per request; throughput is work per unit time. Batching can improve throughput while hurting queueing latency.", "Quantization reduces memory and often latency with possible quality loss.", "Distillation trains a smaller student; pruning removes low-value weights or structure.", "Measure end-to-end percentiles and cost, not only isolated average model time."],
        interviewPrompt: "Choose among quantization, distillation, batching, and a smaller architecture for an on-device model.",
        code: "quantized = torch.quantization.quantize_dynamic(model, {nn.Linear})",
      },
      {
        id: "end-to-end",
        title: "End-to-end ML system reasoning",
        summary: "An ML system is the complete loop from objective and labels through deployment, monitoring, and retraining—not just a neural network.",
        keyPoints: ["Define prediction timing, action, latency, and asymmetric error costs before selecting a metric.", "Design labels and time-aware splits; prevent leakage and account for label delay, imbalance, and distribution shift.", "Build a simple baseline, train candidates, evaluate relevant slices, and perform error analysis.", "Deploy with versioning and rollback; monitor inputs, predictions, latency, drift, and delayed outcomes.", "Close the loop with retraining triggers, human review, and safeguards against feedback loops."],
        interviewPrompt: "Design a fraud or ranking system from business objective through post-deployment monitoring.",
        code: "objective -> data -> baseline -> model -> evaluate -> deploy -> monitor -> retrain",
      },
    ],
    quiz: [],
  },
  {
    id: "generative-models",
    number: "7",
    title: "Generative and representation models",
    description: "Lower-priority concepts worth recognizing and explaining at a high level.",
    color: "#F97316",
    icon: "CircleDot",
    topics: [
      {
        id: "autoencoders",
        title: "Autoencoders",
        summary: "An autoencoder compresses input x into latent code z and decodes z into a reconstruction x̂.",
        keyPoints: ["The reconstruction target is the input itself.", "A bottleneck, noise, sparsity, or another constraint prevents trivial copying and encourages useful structure.", "Uses include dimensionality reduction, denoising, anomaly detection, and representation learning.", "A plain autoencoder is not automatically a good generative model because its latent space may be irregular."],
        interviewPrompt: "Why does an unconstrained overcomplete autoencoder risk learning the identity?",
        code: "reconstruction = decoder(encoder(x))",
      },
    ],
    quiz: [],
  },
];

export const mlTopicsById: Record<string, MlTopic> = Object.fromEntries(
  mlParts.flatMap((part) => part.topics.map((topic) => [topic.id, topic] as const))
);

export const mlPartsById: Record<string, MlPart> = Object.fromEntries(mlParts.map((part) => [part.id, part]));

export const mlPartIdForTopic: Record<string, string> = Object.fromEntries(
  mlParts.flatMap((part) => part.topics.map((topic) => [topic.id, part.id] as const))
);

export const mlTopicCount: number = mlParts.reduce((sum, part) => sum + part.topics.length, 0);
